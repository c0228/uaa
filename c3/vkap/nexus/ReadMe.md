STEP-1:
-----------------------------------------
| composer install						|
-----------------------------------------
a) Reads composer.json
b) Downloads required packages
c) Creates vendor/ folder
d) Creates composer.lock
If dependencies were already installed earlier 
and we changed composer.json, run:
-----------------------------------------
| composer update						|
-----------------------------------------